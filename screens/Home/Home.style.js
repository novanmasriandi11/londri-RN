import { StyleSheet } from "react-native";

export const Styling = StyleSheet.create({
  container: {
    // flex: 0,
    justifyContent: "center",
    // alignItems: "center",
    // backgroundColor: "red",
  },
});
